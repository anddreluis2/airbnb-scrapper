CONCURRENCY=4
PROXY_URL=
MAX_PAGES=50
DELAY_MIN=1500
DELAY_MAX=2500
LISTING_DELAY_MIN=1000
LISTING_DELAY_MAX=2000
MAX_RETRIES=5
OUTPUT_FILE=data/listings.csv

# Timeouts (ms) - maior = menos falhas por timeout
NAVIGATION_TIMEOUT=180000
NAVIGATION_RETRIES=3
PAGINATION_WAIT=30000

# Delays (ms) - page load e scroll
PAGE_LOAD_MIN=800
PAGE_LOAD_MAX=1500
SCROLL_SETTLE_MIN=400
SCROLL_SETTLE_MAX=800

# JSON array of price segments (optional, uses sensible defaults if omitted)
# PRICE_SEGMENTS=[{"min":0,"max":300},{"min":300,"max":600},{"min":600,"max":1000},{"min":1000}]
